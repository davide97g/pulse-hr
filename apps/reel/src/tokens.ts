/**
 * Re-export of shared marketing tokens so reel visuals stay in lockstep with the site.
 * Source of truth: packages/tokens/src/index.ts.
 */
export { color, font, radius } from "@pulse-hr/tokens";
