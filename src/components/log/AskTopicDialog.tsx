// src/components/log/AskTopicDialog.tsx — stub (full impl lands in Task 9)
export function AskTopicDialog(_: {
  employeeId: string;
  open: boolean;
  onOpenChange: (o: boolean) => void;
}) {
  return null;
}
